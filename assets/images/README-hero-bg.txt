Add your hero background image here:
  hero-bg.jpg  (minimum 1920 × 1200 px recommended for parallax)

The hero section uses this image with a dark overlay. Until the file is added,
the gradient overlay will still show but the background will be missing.
